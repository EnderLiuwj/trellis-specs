# 批处理、成本与安全边界

- 请求必须设置超时、有限重试与指数退避；并发、速率限制和最大样本数都应可配置。
- 默认允许本地格式检查、小样本 smoke test 与离线评估；大规模或付费批量调用前，先展示模型、样本数、并发和预计规模，并征得用户确认。
- 输出结构化汇总：成功/失败数、耗时、token 与可得时的估算成本；不记录密钥、敏感输入或完整敏感响应。
- 对可识别个人信息、业务机密和受限数据，默认不发送至外部 API。是否可发送由用户明确确认并遵循实际服务商数据政策。
- 不得自动删除输出或日志；清理前先列出目标并请求确认。
