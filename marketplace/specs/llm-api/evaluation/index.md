# 评估与结果

- `eval_cases/` 保存少量、脱敏且可提交的典型、边界和失败样例，推荐 JSONL 格式。
- 改变提示词、模型、供应商或核心逻辑后，运行 `scripts/eval.sh`；结果写入 `outputs/<run-name>/`。
- 每次结果至少包含实际模型标识、提示词/配置版本、样本数、成功失败数、延迟、token 用量与质量指标。
- 只保存脱敏输出或必要摘要；敏感原文和完整响应仅可保存在项目外的受控位置。
- 如有人工判定，明确评价标准与标注版本；不要将不同样本集或不同提示词版本的结果直接比较。
