# 数据规范

- 原始数据、解压数据和数据缓存不进入 Git；数据根目录通过 `local.env` 的 `DATA_ROOT` 配置，并允许脚本以 `--data-root` 覆盖。
- 明确记录数据集名称、版本、许可证、获取方式、训练/验证/测试划分以及预处理版本。
- 训练、验证和最终测试的划分必须稳定、可复现；禁止以测试集调参或泄漏未来信息。
- 划分文件、类别映射和小型元数据可以提交；任何包含敏感信息的大型样本清单不得提交。
- 数据路径不存在、划分无效或类别数不一致时，训练脚本应在启动阶段失败并给出清晰提示。
